;; TODO: Look at http://common-lisp.net/project/lispbox/

;; automatically byte-compile lisp buffers on save - see http://tsengf.blogspot.com/2011/07/auto-byte-compile-your-emacs.html
;; (add-hook 'emacs-lisp-mode-hook '(lambda ()
;;   (add-hook 'after-save-hook 'emacs-lisp-byte-compile t t)))

(provide 'personal/lisp)
